const SearchSchoolList = ({ schoolName }) => {
	return (
		<>
			<div className="flex w-full h-full text-[16px] mb-1">{schoolName}</div>
			<hr className="border px-[4%]" />
		</>
	);
};

export default SearchSchoolList;
